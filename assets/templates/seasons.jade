.row
    .col-xs-12
        h2 {{show}}
        button.btn.btn-default.btn-block(type="button", ng-click="random()")
            span.glyphicon.glyphicon-random
br
.row
    .col-xs-12
        p(ng-hide="seasons") Loading...
        ul.list-group(ng-show="seasons")
            a.list-group-item(ng-click="showEpisodes(season)", ng-repeat="season in seasons", href="")
                | Season {{season}}